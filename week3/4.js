function vehicleType(color, code) {
    if (code === 1) {
        console.log(color + ' car');
    }
    if (code === 2) {
        console.log(color + ' motorbike');
    }
}
vehicleType("blue", 2);
vehicleType("black", 1);