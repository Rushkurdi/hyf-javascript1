let newarr = [1, 2, 3, " HelloWorld ", false];
console.log("it's possible to store multiple types in an array " + newarr);
let x = 6 / 0;
let y = 10 / 0;
console.log(x);
console.log(y);
if (x == y) {
    console.log("we can compare infinities")
} else {
    console.log("we can't compare infinities")
}